{
  "name": "backstage-retrofit-test",
  "version": "1.0.0",
  "description": "Test repo for Backstage CI/CD injection",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
